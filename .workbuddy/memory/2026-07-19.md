# 2026-07-19 工作日志

## 全面测试任务

**任务：** 用户要求在 `D:\gork\wth` 工作区对 WTH（Wide Thought Host）软件做全面测试。

### 项目识别
- Rust 工作区，82 个 crate
- 来源：从 `xai-org/grok-build` 和 `thedavidweng/gork-build` 衍生
- 主二进制：`wth-pager-bin`（产物名 `wth`）
- CI 门禁见 `.github/workflows/ci.yml`，CONTRIBUTING.md 还列了 `wth-agent --lib` 和 `wth-config --lib`

### 工具链适配（重要！）
- 系统没装 Visual Studio Build Tools，所以 `stable-x86_64-pc-windows-msvc`（默认）不可用
- 必须用 `+stable-x86_64-pc-windows-gnu` 工具链
- GNU 工具链自带的 `dlltool.exe` 在 `lib/rustlib/x86_64-pc-windows-gnu/bin/self-contained/` 下，但 gcc.exe 只是链接器占位符
- 通过 `winget install BrechtSanders.WinLibs.POSIX.UCRT` 装了完整 MinGW-w64（gcc 16.1.0 + dlltool + ld），路径：`/c/Users/21085/AppData/Local/Microsoft/WinGet/Packages/BrechtSanders.WinLibs.POSIX.UCRT_Microsoft.Winget.Source_8wekyb3d8bbwe/mingw64/bin`
- protoc 用项目自带的 `bin/protoc` dotslash 脚本（不支持 Windows），下载了 `protoc-29.3-win64.zip` 放到 `.workbuddy/tmp/bin/protoc.exe`

### 发现的 3 个真实缺陷
1. **P0** — `xai-grok-config` 重命名遗留：`paths.rs` 把 `user_grok_home` 改名为 `user_wth_home`，但 `loader.rs:8` / `managed_cache.rs:11` / `validation.rs:7` 仍用旧名。修复：3 处 import 改名（方案 A）或在 paths.rs 末尾加 `pub use user_wth_home as user_grok_home;`（方案 B）。
2. **P0** — `xai-proto-build/src/lib.rs:117-120` 硬编码 `/dev/stdout` / `/dev/null`，Windows 下 protoc 调用必失败，连带 6 个 crate 编不出来。Linux CI 不暴露。
3. **P2** — `xai-sqlite-journal` 在 Windows 下 3 个测试失败：`effective_db_path_is_per_host_only_in_truncate_mode`、`open_readonly_truncate_never_opens_legacy_file`、`network_mode_survives_legacy_wal_flip_back`。平台特定，Linux CI 不暴露。

### 新增测试
- `crates/codegen/wth-models/src/lib.rs` 末尾追加 `#[cfg(test)] mod tests` — 11 个单元测试，原本 0 测试。包名是 `xai-grok-models`（不是 `wth-models`）。

### 测试结果
- 495 通过 / 3 失败
- 11 个测试任务完全通过，8 个被缺陷 #1 或 #2 阻塞
- 完整报告：`D:\gork\wth\TEST_REPORT.md`
- 测试脚本与日志：`.workbuddy/tmp/run_ci_tests.sh`、`.workbuddy/tmp/test-logs/`

### 项目命名约定提醒
- 目录名 `wth-*` ↔ 包名 `xai-grok-*` / `xai-tool-*` 经常不一致。写 cargo 命令前要查 Cargo.toml 里的 `name` 字段，不要凭目录名猜。
- 重命名 grok → wth 的迁移只做了一半，到处都有这种半成品。

---

## 子项目 1：修复 WTH Windows 编译缺陷（2026-07-19 下午）

**任务：** 修复上一轮发现的 3 个 P0/P2 缺陷，让 `wth-pager-bin` 能在 Windows 上 release 编译并运行。

### 修复的 4 个缺陷
1. **缺陷 #1**：3 处 import + 12 处 unqualified 调用改名 `user_grok_home → user_wth_home`（loader.rs, managed_cache.rs, validation.rs）。保留 `lib.rs:60` 的公共 API 别名。
2. **缺陷 #2**：`xai-proto-build/src/lib.rs` 加跨平台 `NULL_DEV` 常量（NUL/`/dev/null`）+ 用 `tempfile::NamedTempFile` 替代 `/dev/stdout`。改前缀解析为动态 `format!("{NULL_DEV}:")`。
3. **缺陷 #3**：`xai-sqlite-journal` 的 `host_discriminator` 把 sanitize 逻辑抽成独立 `sanitize_hostname` 函数，把 `is_ascii_alphanumeric` 放宽成 Unicode-aware `is_alphanumeric`。新增 2 个回归测试覆盖非 ASCII 主机名（中文 "昊"、日文 "太郎"、德文 "München"、俄文 "МОСКВА" 等）。
4. **缺陷 #4（新发现）**：`xai-grok-update` 的 `test_reinstall_hint_internal_points_at_gork_source_build` 测试期望 "gork"，但 `manual_install_cmd()` 已经改成 "wth-pager-bin"。修测试期望匹配 WTH 项目身份。

### 验收结果
- ✅ `cargo +stable-x86_64-pc-windows-gnu build -p wth-pager-bin --release` 成功，11m 23s，产出 275MB `wth.exe`
- ✅ `wth --version` → `wth 0.1.220-alpha.4 (20b83f72)`
- ✅ `wth --help` → "Wide Thought Host TUI" / "Usage: wth"
- ✅ 上一轮 20 项测试套件重跑：03/05/06/08/09 已确认通过（之前因缺陷 #1/#2 失败）
- ✅ sqlite-journal 18→20 测试全通过（含新增 2 个回归测试）

### 修改的文件
- `crates/codegen/wth-config/src/loader.rs`（缺陷 #1）
- `crates/codegen/wth-config/src/managed_cache.rs`（缺陷 #1）
- `crates/codegen/wth-config/src/validation.rs`（缺陷 #1）
- `crates/build/xai-proto-build/src/lib.rs`（缺陷 #2）
- `crates/codegen/xai-sqlite-journal/src/lib.rs`（缺陷 #3）
- `crates/codegen/xai-grok-update/src/auto_update.rs`（缺陷 #4）
- `docs/superpowers/specs/2026-07-19-fix-windows-compile-defects-design.md`（spec 文档）

### 遗留问题（不在子项目 1 范围）
- `xai-grok-update` crate 里还有大量 "gork" / "Gork Build" 文本未重命名为 "wth" / "Wide Thought Host"（line 28, 38, 39, 41, 79, 80, 82 等）。不影响编译，但影响项目身份一致性。建议作为独立清理任务。
- `wth --help` 输出里仍有 "gork" 字样（如 `gork "fix the bug"` 示例），同上。

### 下一步
子项目 2：Tauri GUI 壳。需要重新 brainstorm，用 WTH `--headless` 模式作为后端。

---

## 子项目 2：补全 wth-desktop 前端 React 组件（2026-07-19 下午）

**重大发现：** 项目里已经有 `wth-desktop` Tauri v2 骨架（`crates/desktop/wth-desktop/`），不需要从零搭建。Rust 后端 564 行 IPC + 前端 IPC/Store/Tailwind 配色都齐全。**只缺 3 个被 App.tsx 引用但不存在的 React 组件。**

### 实现的 3 个组件

1. **`src/components/sidebar/Sidebar.tsx`** — 会话列表（标题 + 相对时间 + 消息数 badge + hover 删除）
2. **`src/components/chat/ChatView.tsx`** — 聊天主区（markdown 渲染 + 代码高亮 + tool call 折叠 + 流式光标 + Enter 发送）
3. **`src/components/filetree/FileTree.tsx`** — 文件树（递归展开 + 按扩展名着色图标）

### 实现中遇到的问题与解决

1. **缺 `icons/icon.ico`** — Tauri Windows 构建需要。写 `.workbuddy/tmp/gen_icons.py` 用 Pillow 从 `docs/assets/gork-build-symbol-white.png` 生成所有图标格式。
2. **`tauri-runtime` 与 `tauri-runtime-wry` 版本不匹配** — Cargo.lock 锁了 tauri-runtime 2.11.3 但 tauri-runtime-wry 2.10.1 没跟上 trait 变化。`cargo update -p tauri-runtime --precise 2.10.1` 降回匹配。
3. **`tauri.conf.json` 的 `frontendDist` 路径不对** — `"../ui/dist"` 改成 `"ui/dist"`。

### 验证结果
- ✅ `npm run build` 成功（2797 模块，dist/ 生成）
- ✅ `cargo build -p wth-desktop` 成功（14.54s，120MB wth-desktop.exe）
- ✅ 启动成功：`INFO Wide Thought Host desktop started` + GUI 窗口创建
- Spec：`docs/superpowers/specs/2026-07-19-tauri-frontend-components-design.md`

### 子项目 2 还遇到并修复的问题
3. **Rust 后端代码跟不上 Tauri v2 API** — 4 处代码修复：3 个文件加 `use tauri::Emitter;`（emit 移到 trait 了）；main.rs:35 `init()` → `Builder::new().build()`；agent.rs:184 `_ = async {` → `result = async {`（漏写变量名）
4. **tauri.conf.json plugins 配置字段不被新版接受** — 删掉 `scope`/`all`/`shortcuts` 等字段，只保留 `shell.open: true`（permissions 已在 capabilities/default.json 里配好）
5. **frontendDist 路径不对** — `"../ui/dist"` → `"ui/dist"`

### 下一步
子项目 3：WorkBuddy 风格 UI 细节复刻。✅ 已完成（见下）

---

## 子项目 3：WorkBuddy 风格 UI 改造（2026-07-19 下午）

通过可视化 brainstorm（localhost:57369）确定设计方向，CSS 变量 + Tailwind 扩展实现双主题。

### 设计决策
- 双主题：深色保留 GitHub Dark（#58a6ff 强调）+ 浅色清爽白紫（#6c5ce7 强调）
- 侧边栏重组为垂直分组导航（会话/文件/设置 + Sun/Moon 主题切换）
- 消息气泡圆角角色区分（用户 rounded-tr-sm、AI rounded-tl-sm）
- 纯色强调（不用渐变）

### 实现
- 策略：CSS 变量驱动 Tailwind（`bg-surface-0` → `var(--surface-0)`）
- 主题切换：`<html class="dark/light">` + localStorage 持久化
- 改动 6 个文件：`tailwind.config.js` / `index.css` / `App.tsx` / `ChatView.tsx` / `Sidebar.tsx` / `FileTree.tsx`

### 验证
- ✅ npm run build（2797 模块，0 TS 错误）
- ✅ cargo build -p wth-desktop（4.58s 增量编译）
- Spec：`docs/superpowers/specs/2026-07-19-workbuddy-ui-style-design.md`

### 下一步
子项目 4：系统托盘 + 桌面快捷方式 + 本地存储。✅ 已完成（见下）

---

## 子项目 4：系统托盘 + 桌面快捷方式 + 本地存储（2026-07-19 15:47）

### 现实盘点
- 系统托盘：✅ 已完成（`tray.rs` — 显示/隐藏、新建会话、退出菜单 + 左键切换）
- 全局快捷键：⚠️ 已配置插件但未注册 Alt+Space
- 桌面快捷方式：⚠️ NSIS bundle 已配置但缺 start_menu_folder
- 本地存储：❌ `session.rs` 4 个函数全是 TODO

### 实现
- Sessions 持久化：`%APPDATA%/com.wth.desktop/sessions.json` JSON 文件
  - `load_sessions()` 启动加载 + `save_sessions()` 每次 mutation 写盘
  - `AppState` 新增 `sessions: Arc<Mutex<Vec<SessionInfo>>>` + `sessions_path: Arc<Mutex<PathBuf>>`
- 全局快捷键：`Shortcut::new(Some(Modifiers::ALT), Code::Space)` + `app.global_shortcut().register()`
- NSIS 打包：`install-mode: currentUser` + `start-menu-folder: Wide Thought Host`（默认自动创建桌面快捷方式）

### 验证
- ✅ cargo build 通过（23.48s）
- Spec：`docs/superpowers/specs/2026-07-19-tray-shortcut-storage-design.md`

### 下一步
子项目 5：exe 打包。✅ 已完成（见下）

---

## 子项目 5：exe 安装包打包（2026-07-19 23:55）

运行 `RUSTUP_TOOLCHAIN=stable-x86_64-pc-windows-gnu tauri build` 成功生成 Windows 安装包。

### 遇到的坑
1. `npx tauri` 找不到 → 用完整路径 `ui/node_modules/.bin/tauri`
2. `beforeBuildCommand` Windows cmd 不兼容 → `"cd ui && npm run build"` → `"npm run build"`
3. 默认 MSVC toolchain 不可用 → 强制 `RUSTUP_TOOLCHAIN=stable-x86_64-pc-windows-gnu`
4. `@tauri-apps/api` 2.11.1 与 Rust tauri 2.10.3 不匹配 → 降级 NPM 到 `~2.10.0`

### 产物
| 文件 | 大小 |
|---|---|
| `target/release/bundle/nsis/Wide Thought Host_0.1.0_x64-setup.exe` | 6 MB |
| `target/release/bundle/msi/Wide Thought Host_0.1.0_x64_en-US.msi` | 9 MB |
| `target/release/wth-desktop.exe` | 32 MB |

### 5 个子项目全部完成 🎉
1. ✅ 修复 WTH Windows 编译缺陷
2. ✅ Tauri GUI 壳
3. ✅ WorkBuddy 风格 UI（双主题 + 分组导航）
4. ✅ 系统托盘 + 全局快捷键 + 本地存储
5. ✅ NSIS/MSI 安装包

Spec：`docs/superpowers/specs/2026-07-19-tauri-build-packaging-design.md`

### 测试后修复（2026-07-20 01:08）
用户安装后报 "WebView2Loader.dll 缺失"。测试发现并修复：

1. **P0 — WebView2Loader.dll 未打包：** GNU 工具链编译的 DLL 在 build 目录但未复制到 release 输出。手动复制 + 修改 `build.rs` 自动检测复制。
2. **P2 — Alt+Space 快捷键冲突 WorkBuddy：** 改为 Alt+W（`main.rs` + `tray.rs`）。
3. 重新 `tauri build` 生成含 WebView2Loader.dll 的安装包。
4. Smoke test 验证：Alt+W 注册成功、sessions 加载正常、启动无异常。

测试报告：`TEST_REPORT_DESKTOP.md`

---

## 第二轮优化：设置+终端+汉化（2026-07-20 09:52）

### 模块 1：设置界面
- 新建 `components/settings/Settings.tsx`（导入为 `SettingsPanel` 避免与 lucide Settings 冲突）
- 功能：模型选择（6 个模型下拉）、主题切换（深色/浅色）、快捷键查看、关于信息
- 默认模型通过 localStorage 持久化

### 模块 2：终端功能
- 新建 `components/terminal/TerminalPanel.tsx`，基于 xterm.js + FitAddon
- 替换 App.tsx 中"即将推出"占位
- 支持多标签终端（新建/关闭），通过 `terminal_spawn`/`terminal_write`/`terminal_kill` IPC 通信

### 模块 3：安装包汉化
- tauri.conf.json 添加 `bundle.windows.nsis.languages: ["SimpChinese", "English"]`
- NSIS 配置字段必须用 camelCase（`displayLanguageSelector` 而非 `display-language-selector`）
- 重新打包：NSIS 6.1MB / MSI 9.1MB（09:52 生成）

### 安装包位置
`target/release/bundle/nsis/Wide Thought Host_0.1.0_x64-setup.exe`

---

## UI 修复 + UI/UX Pro Max 优化（2026-07-20 11:11）

### 1. 设置界面布局修复
**问题：** SettingsPanel 错误地渲染在 224px 宽的侧边栏内，导致内容被挤成竖排文字。
**修复：**
- `App.tsx`：SettingsPanel 移到主内容区（接管全宽）
- 设置时隐藏顶部栏
- `Settings.tsx`：加 `min-h-0` `min-w-0` 防止 flex 溢出
- 右侧详情区独立 `overflow-y-auto`
- 设置行加 `flex-wrap` 防止选项按钮被截断

### 2. UI/UX Pro Max 微交互优化
- `index.css`：添加动画令牌（`--ease-out`、`--duration-fast/normal/slow`）、`:focus-visible` 焦点环、`:selection` 选中文本、行内代码样式
- `Sidebar.tsx`：移除边框指示器，统一 scale 微动效，精简空态
- `ChatView.tsx`：移除用户头像圆圈，消息添加 `animate-fade-in`，发送按钮 scale 反馈
- `App.tsx`：导航按钮加 `duration-150 ease-out` + scale 过渡

**核心理念：** 简化视觉元素、避免冗余装饰、自然专业设计感。
