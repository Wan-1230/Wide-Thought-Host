[package]
license = "Apache-2.0"
name = "wth-version"
version = "0.2.0-dev"
edition.workspace = true
description = "Lockstepped WTH Build CLI version and privacy-build constants."

[lib]
name = "xai_grok_version"

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
