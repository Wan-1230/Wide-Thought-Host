[package]
license = "Apache-2.0"
name = "wth-sampler"
version = "0.1.0"
edition.workspace = true
description = "Actor-based sampling/inference layer for the WTH Build sampling stack (HTTP streaming + retry, no shell coupling)"

[lib]
name = "xai_grok_sampler"

[dependencies]
# Internal
wth-sampling-types = { path = "../xai-grok-sampling-types" }
wth-version = { workspace = true }
wth-circuit-breaker = { workspace = true }

# External
async-openai = { workspace = true }
async-stream = { workspace = true }
chrono = { workspace = true, features = ["serde"] }
eventsource-stream = { workspace = true }
futures-util = { workspace = true }
indexmap = { workspace = true, features = ["serde"] }
reqwest = { workspace = true, features = ["stream"] }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["rt", "macros", "time", "sync"] }
tokio-util = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true, features = ["v4"] }

[dev-dependencies]
# Mock HTTP server for actor / request_task integration tests.
axum = { workspace = true }
# Shared SSE generators for the happy-path mock payloads.
wth-test-support = { workspace = true }
# Breaker test hooks (MockClock) for the sampler breaker integration tests.
wth-circuit-breaker = { workspace = true, features = ["test-hooks"] }
# `start_paused = true` for deterministic timeout testing; `net` for
# `TcpListener::bind` in the mock server harness.
tokio = { workspace = true, features = [
    "rt",
    "macros",
    "time",
    "sync",
    "test-util",
    "net",
    "rt-multi-thread",
] }

[lints]
workspace = true
