[package]
license = "Apache-2.0"
name = "wth-tool-types"
version = "0.1.0"
edition.workspace = true
description = "Canonical tool-description types for the agent tool platform"

[lib]
name = "xai_tool_types"

[features]
# Enables `BuiltinSubagent::render_prompt` (MiniJinja rendering of the
# built-in subagent prompt bodies).
prompt-render = ["dep:minijinja"]

[dependencies]
minijinja = { workspace = true, features = ["custom_syntax"], optional = true }
schemars = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
